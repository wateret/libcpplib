# Range based loop with custom classes

Say we have a class `MyClass`. How can we iterate it with range-based-loop?

Something like below:

```cpp
class MyClass;

MyClass myclass;
for (auto e : myclass) {
    ...
}
```
